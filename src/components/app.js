// src/components/app.jsx

// Import dependencies
import React from 'react'

// Create main App component
const App = () => (
  <div>
    <h1>Welcome to Alpa Desktop</h1>

    <p>Let's start building your awesome desktop app with electron and React!</p>
  </div>
)

// Export the App component
export default App